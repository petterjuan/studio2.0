{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "VM Fitness Hub Backend Data Model",
  "description": "This document defines the data structures for User Profiles stored in Firestore.",
  "definitions": {
    "userProfile": {
      "type": "object",
      "title": "User Profile",
      "description": "Represents a user's profile data stored in the 'users' collection.",
      "properties": {
        "name": {
          "type": "string",
          "description": "The user's full name."
        },
        "email": {
          "type": "string",
          "format": "email",
          "description": "The user's email address."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "The timestamp when the user account was created."
        },
        "isAdmin": {
          "type": "boolean",
          "description": "Flag indicating if the user has administrative privileges.",
          "default": false
        }
      },
      "required": ["name", "email", "createdAt", "isAdmin"]
    }
  }
}
